---
description: Complete diff of all committed changes against base branch
---

### Committed changes (vs base branch)

!`git fetch -q && git --no-pager diff $(git rev-parse --abbrev-ref origin/HEAD)...HEAD`

---
